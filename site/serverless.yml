component: website
org: corentindelannoy2
app: asmat-companion
name: frontend

inputs:
  region: eu-west-1
  domain: companion${env:SITE_DOMAIN_SUFFIX}.allonounouicibebe.fr
  src:
    src: ./
    hook: yarn build
    dist: build
